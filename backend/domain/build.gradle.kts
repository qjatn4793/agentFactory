// domain: pure Kotlin only. No Spring / R2DBC / JPA / Jackson / Validation.
// See .claude/rules/backend/domain-purity.md

dependencies {
    // stdlib + coroutines from root subprojects block
    // nothing else allowed here
}
